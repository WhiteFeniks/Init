# Задание: Используйте выходные данные предыдущей команды, чтобы найти имя и IP-адрес
# устройства, которое устанавливает связь между вами (локальной сетью) и внешним миром.

# Решение: для определения IP-адреса используем команду traceroute slash16.org
# Далее выбираем первый IP (192.168.20.1), после с помощью команды whois 192.168.20.1
# узнаем имя IANA (Internet Assigned Numbers Authority)
