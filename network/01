# Задание: Получите список сетевых интерфейсов машины, 
# не отображая никаких деталей для этих интерфейсов. Только список имен.

# Решение: Информация из man ifconfig
# Флаг -l может использоваться для вывода списка всех доступных интерфейсов
# в системе без дополнительной информации.

ifconfig -l