# Задание: Найти внешний IP 42.fr.

# Решение: nslookup используется для запроса системы доменных имен (DNS) для получения доменного
# имени или сопоставления IP-адреса
# grep -v '#' выбрать строки не содержащие #
# sort - сортируем в порядке возрастания

nslookup 42.fr | grep 'Address'| grep -v '#' | cut -d ' ' -f2 | sort