# Задание: Определите шлюз по умолчанию в таблице маршрутизации (Routing tables)

# Решение: в man для netstat (используется для проверки сетевой конфигурации и активности)
# Опция -r, он показывает таблицу маршрутизации ядра, подготовленную с помощью route.
# Опция -n заставляет netstat печатать IP-адреса вместо имен хостов и сетей (обычно netstat 
# интерпретирует адреса и пытается отобразить их символически).Это особенно полезно, 
# когда вы хотите избежать поисков адреса по сети (например, через DNS или NIS-сервер).
# awk '{print$2}' выбираем только второй столбик

netstat -rn | grep 'default' | awk '{print$2}'
