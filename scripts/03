# Задание: Напишите сценарий, который удалит активного пользователя на виртуальной машине.

# Решение:
# Создает архив tar.gz текущего каталога.
# конструкция ${PWD##*/}, берет полный путь до текущего каталога (переменная $PWD)
# и удаляет из него первую часть вплоть до последнего слеша, оставляя, таким образом,
# только имя самого каталога. Далее к нему добавляется расширение tar.gz.

#!/bin/bash
tar -czf "../${PWD##*/}.tar.gz"
