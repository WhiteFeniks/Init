# Задание: Список всех существующих пользователей на виртуальной машине

# Решение: /etc/passwd — файл, содержащий в текстовом формате список 
пользовательских учётных записей (аккаунтов).

cat /etc/passwd | cut -d ":" -f 1